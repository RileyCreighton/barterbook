# 0.1.0-devnet-preview — Stocklana prototype (draft)

**Current deployment — September 25, 17:24 UTC:** The [Solflare transaction-message signing repair](solflare-transaction-signing.md) is live as build `b45551b`, Worker `dd5624c1-15b3-4115-a30b-06cd53c31aba`. [Served-file verification](../evidence/hosted/solflare-signing-deployment-b45551b.json) passed; [CI passed all 262 tests](../evidence/local/ci-36166658227.json). The owner explicitly approved publication and deployment. Follow the [two-person retry and three-person rehearsal steps](browser-transaction-retest.md). Actual extension settlement remains to be verified. At deployment, version 6 had no saved signatures or transaction ID and still needed reconciliation. The older network-warning investigation below is historical.

**Earlier deployment — September 25, 10:03 UTC:** [signing-feedback UI `9356ad6aa5fc50d5d78840655815687fb9add5a4`](../evidence/hosted/signing-feedback-deployment-9356ad6.json), Worker `f041aa3d-8363-4656-92b3-3f77ff8a2e8f`, is live. The served bundle hash matches the tested build, health reports Devnet, and both earlier SDK receipts remain in public history. This changes signing feedback only; financial checks and transaction bytes are unchanged. The 42 focused wallet/signing/session tests, typecheck and build passed; [CI for `9356ad6` passed all 242 tests](../evidence/local/ci-36121726045.json), with zero failures or pending tests. Solflare transaction compatibility remains blocked and the Free CPU gate remains unmet. The next action is review of the [prepared Solflare diagnostic report](solflare-network-report.md), which has **not been sent**; no Mainnet switch or blind retry is requested.

This release combines selected-inventory listings, exact two-owner basket offers/counteroffers and bounded three-owner fixed-lot matching in one application. It uses React/Vite/TypeScript, one Cloudflare Worker/Hono API, D1 and server-side Solana RPC. No new onchain program or custody wallet is introduced.

The shared settlement path validates every instruction, preserves partial signatures over one immutable message, computes raw integer fees/nets, persists signed attempts before broadcast and recovers original outcomes after uncertain submission. Unsupported mint/account extensions and opposing bilateral transfers of one mint are rejected. Three public devnet mock mints have passed compatibility transfers; all three are now registered in the live app. A five-distinct-asset basket remains disabled.

Initial evidence includes local tests, the SDK signing spike, local workerd/D1 checks, no-wallet browser UI checks including the repaired mobile guide, and actual local LiteSVM Token-2022 basket/ring/rollback execution. These historical records remain local evidence. See [the finishing verification](https://github.com/RileyCreighton/barterbook/blob/main/evidence/finish-verification.json) and [evidence ledger](evidence-and-wallet-smoke.md) for exact scope and subsequent checkpoints.

Release preparation adds beginner Helius/wallet/deployment/GitHub guides, an under-three-minute pitch and submission drafts within freshly verified form limits. CI checks locally; publishing uses a separate manual workflow. Apache-2.0 covers original application source; retained third-party notices and the exact LGPL dependency source accompany deployment assets.

The [public prototype](https://barterbook-devnet.rileycreighton.workers.dev/#/walkthrough) and [source](https://github.com/RileyCreighton/barterbook) are live. Initial commit `629ed20` passed GitHub CI with 143 tests; its production build, real D1 migrations and hosted SDK authentication/persistence checks passed. The earlier unfunded faucet snapshot is historical.

**2026-09-25 07:35 UTC checkpoint:** developer-run SDK tests finalized the public devnet [two-for-one basket](../evidence/devnet/20260925-sdk-proof-01/devnet-two-for-one-receipt.json), [three-way exchange](../evidence/devnet/20260925-sdk-proof-01/devnet-three-way-receipt.json), and [controlled final-leg failure](../evidence/devnet/20260925-sdk-proof-01/devnet-atomic-last-leg-failure-receipt.json). Network fees were `10000`, `15000` and `15000` lamports. The final-leg failure returned instruction 7 / custom error 1 and zero intended token deltas in existing accounts. New-ATA rollback is separately proven in LiteSVM only. These developer-run devnet transactions use SDK keys and mock assets; they do not prove browser signing or the hosted exchange flow.

Nine finalized fixture operations gave each browser participant 1,000 of each TEST-A/TEST-B/TEST-C. Alice and Carol also received 0.05 faucet SOL each in a separate finalized setup transfer. Cloudflare access is restored, all three mints are live, and all three browser authentication records were independently found in D1. The user reports successful Phantom logins and portfolios. Alice's browser-created basket has independently checked terms; at the earlier readiness checkpoint, D1 independently confirmed both participants had accepted version 1 and were READY, with no browser attempt at that time. Settlement is enabled for the authorized devnet rehearsal, while browser transaction signing remains unproven. The earlier owner-reported `GET /holdings` CPU of 2 ms and telemetry 403 preceded the actual collection recorded below. The updated script suite passed 35 focused tests, including 14 new helper regressions; [GitHub CI run 36109186427](https://github.com/RileyCreighton/barterbook/actions/runs/36109186427) completed successfully for commit `b02e90b`, with **158/158 tests passed, 0 failed and 0 pending** in its downloaded test report.

This is a reviewable prerelease, not completed browser-wallet settlement proof or a submitted entry. Mainnet/PRE tests require separate eligibility and asset authorization; fixture-only PRE bounty eligibility remains unconfirmed.

At the earlier registry-deployment checkpoint, `b02e90b` was deployed, Worker version `69e2cdac-ab4d-423b-a424-837d799b1066`, with three mints, six demo participants and SDK proofs. [Its 16 HTTP checks passed](../evidence/hosted/http-smoke-1790323034655.json). New account-list, stale-cookie, mobile and dated-expiry fixes are locally verified but not deployed yet. Latest local checks passed 177 tests and a separate corrected eight-test rehearsal suite, plus build and secret scan; combined CI remains upcoming.

**Historical `f71` rehearsal deployment:** `f71b03decc624965ed74d0d4ff9b8844cb350d5d`, Worker `0714cb11-6087-44db-abaa-c4127f72f61b`. Wallet/session/mobile/expiry fixes are deployed with [passing `46c9` CI](https://github.com/RileyCreighton/barterbook/actions/runs/36111481548). Both history-trust flags are enabled after the archived provider assessment. `f71` fixes the D1 update-result check that returned 409 after the hosted SDK rehearsal's first signature had actually saved. Its original expired attempt `c4bba8a0-95c1-4930-a493-53965a7bbb8e` remains `STATUS_UNKNOWN` without submission-started state; recovery is under diagnosis, and no hosted-settlement completion is claimed. Financial rehearsal CPU remains unmeasured; the owner has not located those dashboard requests. No competition submission or release publication was performed.

**Historical `25cae14` deployment:** `25cae1414a4ce42bf2cb8347c875b311c00dd5c5`, Worker `12e768df-54cb-4a92-95d8-30435f6f2cf5`; [CI passed 194/194 tests](https://github.com/RileyCreighton/barterbook/actions/runs/36113017348). SDK recovery is blocked by the public fallback's HTTP 403 at `getHealth`; the primary is healthy and reports absence after expiry, but that alone does not release the original. Alchemy Free setup/verification is pending, and paid dRPC access was not purchased. Alice/Bob READY is independently confirmed in D1; browser transaction signing is not. [Actual hosted CPU collection](../evidence/hosted/cpu-2026-09-25T08-32-51.375Z.json) has 496 untruncated rows; sparse financial samples (prepare 60 ms, signature 16 ms, reconciliation 15/23 ms) exceed the published 10 ms Free allowance. No CPU-limit outcome was recorded, but the budget gate is not passed. Optimization and remeasurement are pending, with no paid changes. The Alchemy URL is saved privately; provider validation is in progress, with no configured/verified fallback claimed.

**Earlier hosted SDK milestone — September 25, 08:43–08:44 UTC:** commit `837b06c735b8fad6ac6302031a6469be84be28ef`, Worker `f3221118-0847-45f2-9c36-e2799cba82b7`. Alchemy passed [11 read-only checks](../evidence/devnet/20260925-sdk-proof-01/recovery-provider-checks/alchemy-1790325426672-5f5fe58c-7548-4e4b-8ad3-ef0ec142d7a1.json) and is configured as the server-only backup. At 08:38, [original-attempt reconciliation](../evidence/devnet/20260925-sdk-proof-01/hosted-original-reconciliation-1790325517997.json) reached `EXPIRED_UNLANDED`, `safeToRetry: true`, with both providers healthy and trusted; the original journal remains preserved.

A new SDK batch, `hosted-financial-02`, using the normal hosted API and `hosted-judge-02` seed, finalized the [basket](../evidence/devnet/20260925-sdk-proof-01/hosted-hosted-financial-02-basket-receipt.json) at slot `503906415` / 08:43:23 UTC and the [three-way exchange](../evidence/devnet/20260925-sdk-proof-01/hosted-hosted-financial-02-ring-receipt.json) at slot `503906516` / 08:43:40 UTC. Public history now contains two real application receipts. These were disposable SDK signers, **not browser-wallet signing**. The owner's existing Alice/Bob browser rehearsal is in progress; its transaction outcome is pending.

[The new CPU collection](../evidence/hosted/cpu-2026-09-25T08-44-50.289Z.json) contains 55 untruncated rows: prepare 26/52 ms (two samples), signature 9–11 ms (five), submit 10/13 ms (two), and reconciliation 19 ms (four). Invocation outcomes were all `ok`, but several paths exceed the published 10 ms Workers Free allowance. **The Free CPU gate is not met**; optimization has not fully solved it. No paid change was made.


**Earlier recovery deployment — September 25, 09:13 UTC:** [recovery repair `63378c6`](../evidence/hosted/recovery-deployment-63378c6.json), Worker `82b42cd9-aa9c-4111-96e7-666060f13c2d`, was deployed with both explicit address-history trust flags enabled after assessment. The 58 local recovery/settlement tests, typecheck and build passed; [this commit’s CI passed 242/242 tests, with zero failures or pending tests](../evidence/local/ci-36117159889.json). The [original browser attempt safely reconciled](../evidence/hosted/browser-original-reconciled-026ed6d7.json) to `EXPIRED_UNLANDED`, `safeToRetry: true`, with its journal and null transaction identifier preserved. The owner reports same-room version 2 renewal with both participants accepted and ready. The requested Bob-then-Alice transaction approvals and submission remain pending verification. [Hosted recovery CPU](../evidence/hosted/cpu-2026-09-25T09-16-06.908Z.json) was 60 ms in one `ok` sample, so the Free budget remains unmet. This deployment does not establish browser signing or a Free CPU budget pass.

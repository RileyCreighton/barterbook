# Finish the live rehearsal

**Current deployment — September 25, 17:24 UTC:** The [Solflare transaction-message signing repair](solflare-transaction-signing.md) is live as build `b45551b`, Worker `dd5624c1-15b3-4115-a30b-06cd53c31aba`. [Served-file verification](../evidence/hosted/solflare-signing-deployment-b45551b.json) passed; [CI passed all 262 tests](../evidence/local/ci-36166658227.json). The owner explicitly approved publication and deployment. Follow the [two-person retry and three-person rehearsal steps](browser-transaction-retest.md). Actual extension settlement remains to be verified. At deployment, version 6 had no saved signatures or transaction ID and still needed reconciliation. The older network-warning investigation below is historical.

The [public guide](https://barterbook-devnet.rileycreighton.workers.dev/#/walkthrough) and [repository](https://github.com/RileyCreighton/barterbook) are live at commit `9356ad6aa5fc50d5d78840655815687fb9add5a4`, Worker `f041aa3d-8363-4656-92b3-3f77ff8a2e8f`. Settlement is enabled for the authorized devnet rehearsal. The earlier `837b06c` [hosted API basket](../evidence/devnet/20260925-sdk-proof-01/hosted-hosted-financial-02-basket-receipt.json) and [three-way exchange](../evidence/devnet/20260925-sdk-proof-01/hosted-hosted-financial-02-ring-receipt.json) finalized with SDK signers; public history has both actual receipts. **These are not browser-wallet results.** The [latest deployment check](../evidence/hosted/signing-feedback-deployment-9356ad6.json) verifies the served bundle matches the tested signing-feedback UI build; financial behavior is unchanged. All 42 focused wallet/signing/session tests, typecheck and build passed; [this commit’s CI passed all 242 tests](../evidence/local/ci-36121726045.json), with zero failures or pending tests.

Alchemy is verified and configured. [The earlier SDK attempt](../evidence/devnet/20260925-sdk-proof-01/hosted-original-reconciliation-1790325517997.json) reconciled to `EXPIRED_UNLANDED`, `safeToRetry: true`, with its journal preserved. Alice/Bob's browser room was independently verified READY before the owner began signing. Its stopped browser attempt `026ed6d7-5368-455e-a1b2-c0963cdeeab7` [safely reconciled](../evidence/hosted/browser-original-reconciled-026ed6d7.json) to `EXPIRED_UNLANDED`, `safeToRetry: true`, after both complete trusted history checks. Its null transaction identifier and original journal are preserved. This is recovery proof, not browser settlement. [Latest recovery CPU](../evidence/hosted/cpu-2026-09-25T09-16-06.908Z.json) was 60 ms in one successful sample and exceeds the published Free allowance; the budget gate is not passed and no paid service was enabled.

**Current Solflare-in-Zen checkpoint:** the owner reports that all three wallets use the official Solflare Firefox extension in their existing Zen profiles, show their exact original public addresses, and complete BarterBook login. Displayed Devnet balances are Bob **1 SOL**, Alice **0.05 SOL** and Carol **0.05 SOL**. These extension and login observations are owner reports, not a new independent D1 authentication verification.

The [latest independent D1/RPC diagnostic](../evidence/hosted/solflare-network-diagnostic-256c575e.json) records actual terms **version 5**, attempt `256c575e-b91b-4414-96da-ac9d8587129f`. The owner reports Bob’s **Solflare 2.28.1 in Zen** displayed “Network mismatch”: current network Devnet, transaction Mainnet. No signature was stored. After the prompt was closed and reconciliation ran, D1 independently showed `EXPIRED_UNLANDED`, `safeToRetry: true`, a null transaction identifier and complete absence checks from both trusted histories. Both providers returned the full Devnet genesis and the frozen blockhash matched its context-slot block. This establishes the blockhash’s Devnet origin, not the warning’s cause. [Static inspection of the public Solflare package](../evidence/hosted/solflare-2.28.1-compatibility-review.json) establishes no secure workaround or definite cause; it does not test installed wallet state or execute the package. Complete browser compatibility and a new receipt remain unestablished. No Chrome test occurred. Preserve the original and investigate without switching to Mainnet or bypassing verification. The [version 3 safe-expiry record](../evidence/hosted/browser-third-attempt-reconciled-2383931c.json) remains historical evidence.

The earlier Phantom isolation check and [withdrawn Chrome report](../evidence/hosted/browser-second-attempt-owner-report-correction-77036da8.json) remain historical. Bob’s earlier stored signature and [second-attempt safe-expiry record](../evidence/hosted/browser-second-attempt-reconciled-77036da8.json) are separate evidence; the latter’s `ownerReport.chromeDiagnostic` must be read with its correction.

## Account steps for the owner

1. **Account setup is complete.** The CPU token and Alchemy URL are stored privately. Alchemy passed [11 read-only checks](../evidence/devnet/20260925-sdk-proof-01/recovery-provider-checks/alchemy-1790325426672-5f5fe58c-7548-4e4b-8ad3-ef0ec142d7a1.json), is configured as a Worker secret, and successfully participated in original-attempt recovery. Do not re-enter credentials, buy a plan or request more faucet funds. The [backup guide](backup-rpc.md) remains a setup reference.
2. **Keep the CPU limitation visible.** The [latest 144-row collection](../evidence/hosted/cpu-2026-09-25T09-51-25.797Z.json) measured prepare at 48 ms, renewal at 28 ms and fresh assets at 4–5 ms, with no signature POST observed in that window. All outcomes were `ok`; the published 10 ms Free budget remains unmet. The earlier original-message recovery measured 60 ms. A route absent from the collection is not a passing CPU result.
3. **Preserve version 5 and investigate Solflare’s network warning.** The actual latest terms were version 5, not the planned version 4. Bob closed Solflare’s mismatch prompt without a stored signature, and that original attempt has independently verified safe-expiry status. Stay on Devnet and retain the same addresses and archived attempt. No further signing workaround or replacement is established by this warning; do not switch to Mainnet, disable checks or infer a completed exchange. The next action is to review the [prepared Solflare diagnostic report](solflare-network-report.md), which has not been sent. Do not blindly rerun the failed flow. Full Solflare signing compatibility remains unresolved.
4. **Keep all three wallets on Devnet.** Automatic confirmations stay off. Existing funding and TEST-A/B/C inventory suffice. Browser transaction success is not established until the actual signature round trips and its own metadata receipt are verified.
5. **Optional GitHub deployment credentials remain separate.** Follow [the workflow guide](github-release.md) only if configuring that manual deployment token; CPU access does not require deployment permissions.

## Numbered demonstration rehearsal

1. Inspect the original funding and transaction journals. Continue the existing run `20260925-sdk-proof-01`; never delete journals or regenerate wallets to escape an unresolved attempt.
2. Fixture setup is complete: TEST-A/B/C passed fee-aware devnet transfers, browser inventory was issued, and all three mints are registered in the hosted app. Preserve the existing run and its journals. Use [the fixture guide](../scripts/DEVNET.md) only for reconciliation or a separately authorized replenishment batch; no browser private key is needed.
3. The SDK basket, cycle and isolated failing-final-leg test are now finalized with real signatures and raw metadata in the existing run. The later hosted API batch also finalized both successful exchanges; all of these prove SDK execution only. Resuming the same run reconciles its original transactions; use the run/batch instructions for a separately authorized fresh demonstration. The failure script is separate from the public app and may incur a small faucet-funded network fee.
4. The wallet/session/mobile/expiry fixes and authorized devnet settlement switch are deployed. Inspect CPU on actual financial requests without treating the earlier holdings measurement as proof. Use the **same public URL** in Alice, Bob and Carol's separate profiles. A login message proves identity; it does not transfer tokens.
5. Once the network-warning investigation supports another coordinated rehearsal, continue Alice's existing browser offer (room beginning `4cff89`): Alice gives **10 TEST-A + 5 TEST-B**; Bob gives **20 TEST-C** and is the fee payer. Those exact terms were independently checked in D1; [the earlier D1 snapshot independently confirmed both accepted version 1 and were ready](../evidence/hosted/browser-readiness-1790324985916.json), before the now-reconciled browser attempt was created. The owner reports both accepted version 2 and marked ready. Bob's signature is stored and the second attempt safely reconciled. The earlier Phantom warning prompted the current Solflare-in-Zen path; all three Solflare setups/logins are owner-reported; versions 3 and 5 have independently verified safe-expiry outcomes, and version 5 stored no signature after the Solflare network warning. No next signing workaround is established. Browser transaction completion remains unproven. The authorized signature round must preserve one new frozen message and the archived original attempt. With the fixture's 1% schedule, nets are **9.9 A + 4.95 B** to Bob and **19.8 C** to Alice. Verify finalized transaction metadata and download its evidence. The separate [SDK-seeded board demo](../evidence/devnet/20260925-sdk-proof-01/board-hosted-judge-01.json) is not this browser offer.
6. After the browser basket and its recovery are verified, create exact-lot listings: Alice gives **10 A**, wants at least **19.8 B**; Bob gives **20 B**, wants at least **14.85 C**; Carol gives **15 C**, wants at least **9.9 A**. Open the discovered three-owner room. All three accept, mark ready and sign unchanged message bytes. Transfers are Alice → Carol → Bob → Alice. Inspect the one actual finalized receipt.
7. Record a separate declined wallet prompt. It must remain cancelled/recoverable with no fabricated onchain receipt. If any signatures escaped the browser, preserve the original attempt and reconcile its lifetime/status before preparing a replacement. Separately record the real failed SDK transaction as an onchain failure, not as a wallet rejection or simulation result.
8. Refresh the room and public history; verify persistence in separate profiles. Replenish only through a new named idempotent batch. Create several fresh, genuinely backed demo offers through authenticated flows. Keep all old receipts. Explain that developer counterparties must be online to approve them.
9. Record the [pitch](pitch-outline.md) in at most three minutes, optionally the separate five-minute technical walkthrough. Link only actual videos, repository, site and receipts. Update the draft from the evidence actually obtained. Only the owner can supply team attestations, select eligible tracks and authorize final submission. No entry has been submitted.

The original deadline is **September 25, 2026, 4 p.m. Eastern**. Verify the current form again before submitting. If it closes first, finish the prototype and ask the organizer about available options; a late entry is not promised.

# Finish the live rehearsal

The [public guide](https://barterbook-devnet.rileycreighton.workers.dev/#/walkthrough) and [repository](https://github.com/RileyCreighton/barterbook) are live. Cloudflare sign-in, D1 migrations and Helius secrets are configured. The first [GitHub CI run](https://github.com/RileyCreighton/barterbook/actions/runs/36104365100) passed. **Settlement is still disabled.** No completed public devnet exchange or real extension signing proof exists yet.

## Account steps for the owner

1. **Fund the disposable setup wallet from a free faucet.** Open [the official faucet](https://faucet.solana.com/) in your own browser, keep **devnet** selected, paste `5LjQfnCwojPVdqHYjdmZzdnyhzSNottxSZSBGwVkqvAV`, select 1 SOL if offered, and confirm the free airdrop. This address belongs to the existing disposable SDK setup wallet, not an existing funded personal wallet. The faucet currently displays a maximum of two requests per eight hours; follow its actual limit. Do not pay a faucet or transfer real SOL. Tell the builder whether it succeeds; a fresh finalized check must confirm funding before setup continues. The automatic attempt was recorded and will not be blindly repeated.
2. **Create three separate browser participants.** Follow [the exact Chrome/Phantom setup guide](wallet-rehearsal.md). Use new wallets in profiles labeled Alice, Bob and Carol, select Solana Devnet, and keep automatic confirmations off. Send only their three public Solana addresses. Phantom is a candidate to test, not an advertised verified integration. Each browser fee payer also needs free faucet Devnet SOL. Never send a seed phrase, private key or wallet export.
3. **Confirm Workers Free and read hosted CPU.** In Cloudflare, open Workers & Pages → `barterbook-devnet` → Observability. Follow [the CPU guide](hosted-cpu.md) to inspect actual invocation CPU for the test window, grouped by API path. The existing login cannot query this API automatically (403). A screenshot/export from the dashboard is useful; it must include real CPU values, not page load or RPC wait time. If the account asks for an upgrade, stop rather than purchase it. Actual financial paths will need another measurement during the authorized rehearsal.
4. **Enable future GitHub deployments securely.** The `devnet` GitHub environment and its nine public variables are already configured; the account ID secret is set. Only `CLOUDFLARE_API_TOKEN` is missing. In Cloudflare → profile → API Tokens, start from Edit Workers, restrict to this account, include D1 edit access, and copy the token directly into [GitHub repository settings](https://github.com/RileyCreighton/barterbook/settings/environments) → `devnet` → Add environment secret → `CLOUDFLARE_API_TOKEN`. Do not send it in chat. See [the complete workflow guide](github-release.md). The manual workflow has not yet been run. Ordinary pushes only run CI.

## Numbered demonstration rehearsal

1. Inspect the original funding and transaction journals. Continue the existing run `20260925-sdk-proof-01`; never delete journals or regenerate wallets to escape an unresolved attempt.
2. After finalized funding, run the [fixture setup](../scripts/DEVNET.md) with `--run 20260925-sdk-proof-01 --execute --resume`. Each of TEST-A/B/C must pass its own fee-aware devnet transfer before the registry becomes usable. Add the three browser public addresses through the documented recipient manifest. No browser private key is needed.
3. Run the SDK basket, cycle and isolated failing-final-leg test, preserving their real signatures and raw metadata. These prove programmatic execution only. The failure script is separate from the public app and may incur a small faucet-funded network fee.
4. Register the actual validated mints and all developer-controlled public participants. Enable only the authorized Devnet rehearsal after preliminary hosted checks; then inspect CPU on the actual financial requests. Open the **same public URL** in Alice, Bob and Carol’s separate profiles, connect, and authenticate. A message signature proves identity; it does not transfer tokens.
5. Create the two-for-one offer through the normal app: Alice gives **10 TEST-A + 5 TEST-B**; Bob gives **20 TEST-C**. Both review and accept terms, mark ready, and each explicitly approves the same frozen transaction in the wallet. With the fixture’s 1% schedule, nets are **9.9 A + 4.95 B** to Bob and **19.8 C** to Alice. Verify finalized transaction metadata and download its evidence.
6. Create exact-lot listings: Alice gives **10 A**, wants at least **19.8 B**; Bob gives **20 B**, wants at least **14.85 C**; Carol gives **15 C**, wants at least **9.9 A**. Open the discovered three-owner room. All three accept, mark ready and sign unchanged message bytes. Transfers are Alice → Carol → Bob → Alice. Inspect the one actual finalized receipt.
7. Record a separate declined wallet prompt. It must remain cancelled/recoverable with no fabricated onchain receipt. If any signatures escaped the browser, preserve the original attempt and reconcile its lifetime/status before preparing a replacement. Separately record the real failed SDK transaction as an onchain failure, not as a wallet rejection or simulation result.
8. Refresh the room and public history; verify persistence in separate profiles. Replenish only through a new named idempotent batch. Create several fresh, genuinely backed demo offers through authenticated flows. Keep all old receipts. Explain that developer counterparties must be online to approve them.
9. Record the [pitch](pitch-outline.md) in at most three minutes, optionally the separate five-minute technical walkthrough. Link only actual videos, repository, site and receipts. Update the draft from the evidence actually obtained. Only the owner can supply team attestations, select eligible tracks and authorize final submission. No entry has been submitted.

The original deadline is **September 25, 2026, 4 p.m. Eastern**. Verify the current form again before submitting. If it closes first, finish the prototype and ask the organizer about available options; a late entry is not promised.
